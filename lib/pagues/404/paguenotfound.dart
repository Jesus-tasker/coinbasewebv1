import 'package:flutter/material.dart';

class Paguenotfound extends StatelessWidget {
  const Paguenotfound({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("pagina no encontrada"));
  }
}
